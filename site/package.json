{
  "name": "photosite-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build:manifest && astro dev",
    "build:manifest": "node ./scripts/build-manifest.mjs",
    "build": "npm run build:manifest && astro build",
    "preview": "astro preview",
    "check": "npm run build:manifest && astro-check"
  },
  "dependencies": {
    "astro": "^5.2.0",
    "js-yaml": "^4.1.0",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20 <25"
  }
}
